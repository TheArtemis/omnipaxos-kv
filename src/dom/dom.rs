use crate::dom::config::DomConfig;

pub struct Dom {
    config: DomConfig,


    // Early buffer

    // Late buffer

    // Release messages job

    // Slow path if late

    // Ack Proxy on log append   

}